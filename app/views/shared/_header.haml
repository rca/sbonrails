.header
    %h1.logo <span>on</span> Rails

    %ul#menu
        %li
            = link_to "home",root_url, :class => "#{ 'active' if controller.controller_name == 'home' }"
        %li
            = link_to "about", "#", :class => "#{ 'active' if controller.controller_name == 'about' }"
        %li
            = link_to "members", "#", :class => "#{ 'active' if controller.controller_name == 'users' }"
        %li
            = link_to "events", "#", :class => "#{ 'active' if controller.controller_name == 'events' }"
        %li
            = link_to "presentations", "#", :class => "#{ 'active' if controller.controller_name == 'presentations' }"

        %li
            - if current_user
                = link_to "sign out", sign_out_path
            - else
                = link_to "sign in", sign_in_path, :class => "#{ 'active' if controller.controller_name == 'sessions' }"

